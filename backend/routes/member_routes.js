const express = require('express');
const router = express.Router();//라우터 함수 실행
const User = require("backend/schemas/user");


//로그인
// router.get('/login', (req, res) => {
//     res. send('로그인 페이지')
// })

//라우터 내보내기
module.exports = router;